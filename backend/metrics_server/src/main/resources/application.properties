spring.application.name=metrics_server
server.port=8081
management.endpoints.web.exposure.include=*
management.endpoint.shutdown.enabled=true
endpoints.shutdown.enabled=true
appserver.profile.server-ip=${CROPSENSE_APPLICATION_SERVER_HOST}
appserver.profile.server-port=${CROPSENSE_APPLICATION_SERVER_PORT}